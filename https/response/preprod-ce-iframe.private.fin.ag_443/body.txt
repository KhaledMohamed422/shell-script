<!DOCTYPE html>
<html lang="en">
<head>
    <title>FlinksConnect - Connect FIs with modern app</title>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <style>
        html, body {
            margin: 0;
            padding: 0;
            background-color: transparent;
            overflow: hidden;
        }

        .light-bg {
            background-color: #f7f7f7;
        }

        .dark-bg {
            background-color: #1c3265;
        }

        .pre-loading-light-bg {
            background-color: rgba(255,255,255,0.5);
        }

        .pre-loading-dark-bg {
            background-color: rgba(0,0,0,0.5);
        }

        .pre-loading-light-text {
            color: rgba(255,255,255,0.5);
        }

        .pre-loading-dark-text {
            color: rgba(0,0,0,0.5);
        }

        .pre-loading-wrapper {
            padding-top: 50px;
        }

        .pre-loading {
            color: rgba(0,0,0,0.5);
            margin: 2rem auto;
            text-align: center;
        }

        .pre-loading-text {
            font-family: arial;
            font-size: 1em;
            margin: 2rem auto;
            text-align: center;
        }

        .spinner-grow {
            display: inline-block;
            width: 2rem;
            height: 2rem;
            vertical-align: text-bottom;
            border-radius: 50%;
            opacity: 0;
            animation: spinner-grow .75s linear infinite;
        }

        #content > div {
            animation: fadein 0.5s;
        }

        @keyframes spinner-grow {
            0% {
                transform: scale(0);
            }

            50% {
                opacity: 1;
            }
        }

        @keyframes fadein {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .sr-only {
            position: absolute;
            width: 1px;
            height: 1px;
            padding: 0;
            overflow: hidden;
            clip: rect(0, 0, 0, 0);
            white-space: nowrap;
            border: 0;
        }
    </style>

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.6/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">

    <!-- Polyfill -->
    <script src="https://cdn.polyfill.io/v3/polyfill.min.js?features=fetch,IntersectionObserver,URL,Object.fromEntries"></script>

    <!-- Begin GA Code -->
    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date(); a = s.createElement(o),
                m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-83575816-2', 'auto');
        ga('send', 'pageview');
        ga('send', 'event', 'KPI', 'App', 'initiated');
    </script>
    <!-- End GA Code -->

    <!-- Begin GAV4 Code -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-BG25222WGQ"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag() { dataLayer.push(arguments); }

        ga(function (tracker) {
            gtag('js', new Date());
            gtag('config', 'G-BG25222WGQ', {
                'allow_google_signals': false,
                'allow_ad_personalization_signals': false,
                'debug_mode': false
            });
            gtag('event', 'initiated', { 'event_location': 'global', 'event_type': 'app', 'clientId': clientId, 'event_timestamp': Date.now() });
        });
    </script>
    <!-- End GAV4 Code -->

    <script type="text/javascript">
        sessionStorage.clear();

        var windowWidth = window.screen.availWidth;
        var windowHeight = window.screen.availHeight;
        var isRobot = false;

        if (windowWidth === 0 && windowHeight === 0) {
            isRobot = true;
        }

        if (isRobot) {
            //
        } else {
            /*
            (function (n, t, a, e, co) {
                var i = "aptrinsic"; n[i] = n[i] || function () {
                    (n[i].q = n[i].q || []).push(arguments)
                }, n[i].p = e; n[i].c = co;
                var r = t.createElement("script"); r.async = !0, r.src = a + "?a=" + e;
                var c = t.getElementsByTagName("script")[0]; c.parentNode.insertBefore(r, c)
            })(window, document, "https://web-sdk.aptrinsic.com/api/aptrinsic.js", "AP-ZZFRELIU2YQE-2-3");
            */
        }
    </script>

    <!-- IE11 isInteger polyfill -->
    <script>
        Number.isInteger = Number.isInteger || function (value) {
            return typeof value === "number" &&
                isFinite(value) &&
                Math.floor(value) === value;
        };
    </script>


    <link href="/favicon.ico" rel="icon" async type="image/png">
<link href="/css/style.dc91d057b8fee9bf62983d2aa2ef1483.css" rel="stylesheet"></head>

    <body id="body">
        <div id="content">
            <div class="pre-loading-wrapper">
                <div class="pre-loading">
                    <div id="spinner" class="spinner-grow" role="status"></div>
                </div>

                <div id="loadingText" class="pre-loading-text"></div>
            </div>
        </div>

        <script type="text/javascript">
            var staticLoading = getUrlParam('staticLoadingEnable', false);
            var loadingText = document.getElementById('loadingText');
            var spinner = document.getElementById('spinner');
            var theme = getUrlParam('theme', 'dark');
            var lang = getUrlParam('language', 'en');
            var bgColor = getUrlParam('backgroundColor', undefined);
            var textColor = getUrlParam('foregroundColor1', undefined);
            var version = getUrlParam('version', '1');

            if (staticLoading === 'true') {
                loadingText.innerHTML = 'Establishing a secure connection';

                if (version === '2') {
                    theme = 'light';
                }

                if (theme == 'light') {
                    if (version === '1') {
                        body.classList.add('light-bg');
                    }
                    spinner.classList.add('pre-loading-dark-bg');
                    loadingText.classList.add('pre-loading-dark-text');
                } else {
                    if (version === '1') {
                        body.classList.add('dark-bg');
                    }
                    spinner.classList.add('pre-loading-light-bg');
                    loadingText.classList.add('pre-loading-light-text')
                }

                if (bgColor) {
                    document.body.style.backgroundColor = "#" + bgColor;
                }

                if (textColor) {
                    spinner.style.backgroundColor = "#" + textColor;
                    loadingText.style.color = "#" + textColor;
                }

                if (lang == 'fr') {
                    loadingText.innerHTML = 'Chargement d\'une connexion sécurisée';
                }
            }


            function getUrlParam(parameter, defaultvalue) {
                var urlparameter = defaultvalue;
                if (window.location.href.indexOf(parameter) > -1) {
                    urlparameter = getUrlVars()[parameter];
                }
                return urlparameter;
            }

            function getUrlVars() {
                var vars = {};
                var parts = window.location.href.replace(/[?&]+([^=&]+)=([^&]*)/gi, function (m, key, value) {
                    vars[key] = value;
                });
                return vars;
            }
        </script>
    <script type="text/javascript" src="/js/bundle.19ee3993ff876e9c7a4a.js"></script></body>
</html>



https://preprod-ce-iframe.private.fin.ag:443