<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Redirecting...</title>
    <meta name="robots" content="noindex" />
    <link rel="canonical" href="https://unifi.ui.com/network-servers" />
    
    <script>
      // Client-side redirect for the index route of https://network.unifi.ui.com
      // Other routes and assets not affected - https://network.unifi.ui.com/:version/unifi/site/?d=:consoleId

      const mockLocationHref = window.location.href.replace('/#/', '/')
      var url = new URL(mockLocationHref);

      var envDomain = 'ui.com';
      var hostnameParts = url.hostname.split('.');
      var unifiPartIndex = hostnameParts.indexOf('unifi') 
      if (unifiPartIndex > 0) {
        envDomain = hostnameParts.slice(unifiPartIndex+1).join('.');
      }

      var newOrigin = `https://unifi.` + envDomain;
      var inviteCode = url.searchParams.get('invite_code');
      var deviceId = url.searchParams.get('device_id');

      if (inviteCode && deviceId) {
        /** @see /#/invitation?invite_code=:token&device_id=:serverId */
        window.location.href = [newOrigin, 'invite-network-server', inviteCode, deviceId].join('/');
      } else {
        /** @see / */
        window.location.href = [newOrigin, 'network-servers'].join('/');
      }
    </script>
  </head>
</html>

0




http://network.unifi.ui.com:80